// X/Twitter reuses the Open Graph card verbatim — same 1200×630 composition,
// same generator. Re-exported rather than duplicated.
export { default, alt, size, contentType } from "./opengraph-image";
